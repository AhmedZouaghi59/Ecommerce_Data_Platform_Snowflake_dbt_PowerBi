name: ecommerce_dbt
version: 1.0.0
config-version: 2

profile: dbt

model-paths:
  - models

analysis-paths:
  - analyses

test-paths:
  - tests

seed-paths:
  - seeds

macro-paths:
  - macros

snapshot-paths:
  - snapshots

models:
  ecommerce_dbt:

    DWH:
      +materialized: table

    SEM:
      +materialized: view